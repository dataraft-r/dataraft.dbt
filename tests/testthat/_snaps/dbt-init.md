# starter refuses to overwrite existing files

    Code
      dr_dbt_init(root, dr_lake_config(backend = "duckdb"))
    Condition
      Error in `dr_dbt_init()`:
      ! Choose a new or empty directory; existing files are never overwritten.


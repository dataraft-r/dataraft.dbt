% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbt.R
\name{dr_dbt_build}
\alias{dr_dbt_build}
\alias{dr_dbt_test}
\title{Build or test SQL models with dbt}
\usage{
dr_dbt_build(
  project,
  select = NULL,
  exclude = NULL,
  full_refresh = FALSE,
  vars = list(),
  echo = TRUE,
  timeout = Inf,
  stop_on_failure = TRUE,
  catalog = NULL
)

dr_dbt_test(
  project,
  select = NULL,
  exclude = NULL,
  vars = list(),
  echo = TRUE,
  timeout = Inf,
  stop_on_failure = TRUE,
  catalog = NULL
)
}
\arguments{
\item{project}{A \code{\link[=dr_dbt_project]{dr_dbt_project()}} specification.}

\item{select, exclude}{Optional character vectors of dbt selection
expressions.
Each element is one CLI argument; expressions starting with \code{-} are
rejected.}

\item{full_refresh}{Logical scalar. Rebuild incremental models when \code{TRUE}.}

\item{vars}{A named list of non-secret dbt variables, encoded as JSON.}

\item{echo}{Logical scalar. Stream dbt output to the R console.}

\item{timeout}{Positive timeout in seconds, or \code{Inf} for no limit.}

\item{stop_on_failure}{Logical scalar. Raise \code{dr_dbt_failed} on a nonzero
exit
status, failed nodes or missing artifacts. The condition's \code{result} field
retains diagnostics. Set \code{FALSE} to inspect failures as ordinary results.}

\item{catalog}{Optional \code{\link[dataraft.adapters:dr_catalog_openmetadata_dbt]{dataraft.adapters::dr_catalog_openmetadata_dbt()}} adapter, a function
receiving the dbt result, or an S3 catalog whose \code{dataraft.core::dr_capabilities()} declares
\code{metadata_inputs = "dr_dbt_result"} and implements \code{dataraft.core::dr_publish_metadata()}.
After dbt
finishes, hand this invocation's artifacts to OpenMetadata's ingestion
engine. A delivery failure warns and leaves the dbt outcome unchanged.
Retry with \code{dataraft.core::dr_publish_metadata(catalog, result)} without rebuilding models.}
}
\value{
A \code{dr_dbt_result} list with \code{status} (integer exit code), \code{success}
(logical), \code{command}, \code{results} (node tibble), parsed \code{manifest},
\code{artifacts_dir}, \code{stdout}, \code{stderr}, \code{artifact_error}, \code{invocation_id},
\code{artifact_hashes}, original \code{project}, resolved \code{source_bindings},
\code{source_catalog} and optional \code{catalog_delivery}. Warnings reported
by dbt are retained and do not by themselves count as failure.
}
\description{
Run the dbt CLI in a separate process and read that invocation's artifacts.
Each invocation gets a new directory under \code{.dataraft/runs} in the project;
a failed invocation can never reuse an earlier run's successful results.
}
\details{
Close R connections to the same local DuckDB or DuckLake catalog before
running dbt, then reconnect for analysis. dbt manages its own connections.
Anonymous usage telemetry is disabled for the child process.
A dbt build is not an atomic dataraft release: earlier models can have been
materialized even if a later model or test fails. dbt logs and artifacts can
contain SQL, paths and database messages; treat them as project data.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("DATARAFT_DBT_EXAMPLE_PROJECT"))) withAutoprint(\{ # examplesIf}
project <- dr_dbt_project(Sys.getenv("DATARAFT_DBT_EXAMPLE_PROJECT"))
result <- dr_dbt_build(project, select = "tag:reporting", echo = FALSE)
dr_dbt_status(result)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=dr_dbt_status]{dr_dbt_status()}}, \code{\link[=dr_dbt_lineage]{dr_dbt_lineage()}}, \code{\link[=dr_dbt_model]{dr_dbt_model()}}
}

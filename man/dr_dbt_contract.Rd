% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbt-contract.R
\name{dr_dbt_contract}
\alias{dr_dbt_contract}
\title{Export an R contract as dbt model properties}
\usage{
dr_dbt_contract(
  contract,
  name,
  enforced = TRUE,
  types = NULL,
  unsupported = c("error", "report")
)
}
\arguments{
\item{contract}{A \code{\link[dataraft.core:dr_contract]{dataraft.core::dr_contract()}} specification.}

\item{name}{dbt model identifier.}

\item{enforced}{Whether dbt should enforce the exact model schema.}

\item{types}{Optional named character vector overriding SQL types by column.
For example, \code{c(amount = "decimal(18,2)")}. Nested R list columns require
an explicit SQL type because their shape cannot be inferred from \code{list}.}

\item{unsupported}{How to handle checks that cannot be translated:
\code{"error"} (default) or \code{"report"} with a warning and metadata.}
}
\value{
A regular list with \code{version = 2} and one \code{models} entry, suitable
for \code{yaml::write_yaml()} or composition into dbt model properties.
}
\description{
Create an ordinary YAML-ready list containing explicit column types,
not-null tests and a single-column unique-key test. The default SQL types
target dbt-duckdb. Other adapters can supply explicit \code{types} overrides.
This does not install dbt packages, modify a project or execute validation.
}
\details{
Schema enforcement is supported for SQL models with compatible dbt
materializations. It enforces the exact exported names and types. Required
fields and keys use dbt data tests, whose execution is the responsibility of
\verb{dbt build} or \verb{dbt test}; no database constraint guarantee is implied.

R functions, formulas, pointblank rules and composite-key checks are not
translated. By default they cause an error. With \code{unsupported = "report"},
a warning names them and \code{config.meta.dataraft.untranslated} records them
in the exported model. Missing SQL types always cause an error. R lifecycle
policies such as empty-data, freshness and additional-column handling are
recorded separately under \code{r_policies}; they still require R validation.
The export is deliberately a bounded schema bridge, not a replacement for
the original contract or its quality engines.
}
\examples{
schema <- dataraft.core::dr_contract(columns = c(id = "integer", amount = "numeric"), key = "id")
properties <- dr_dbt_contract(schema, "orders")
properties$models[[1]]$columns
}
\seealso{
\code{\link[=dr_dbt_sources]{dr_dbt_sources()}}, \code{\link[=dr_dbt_init]{dr_dbt_init()}}
}

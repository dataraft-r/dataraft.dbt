% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbt-publish.R
\name{dr_dbt_publish}
\alias{dr_dbt_publish}
\title{Snapshot and publish one dbt relation as a governed release}
\usage{
dr_dbt_publish(
  lake,
  result,
  model,
  contract = NULL,
  asset = NULL,
  code_version = NULL,
  version = NULL,
  layer = NULL,
  business_date = NA_character_,
  notify = NULL,
  stop_on_failure = TRUE
)
}
\arguments{
\item{lake}{Connected lake or \code{\link[dataraft.lake:dr_lake_config]{dataraft.lake::dr_lake_config()}} for the database used by dbt.
Caller connections stay open; connections opened from a config are closed.}

\item{result}{Successful result from \code{\link[=dr_dbt_build]{dr_dbt_build()}}.}

\item{model}{Exact dbt unique ID, such as \code{"model.shop.customer_revenue"}, or
an unambiguous node name such as \code{"customer_revenue"}. Selection expressions
and ambiguous names are rejected.}

\item{contract}{Optional contract for the copied candidate. An unnamed
contract is scoped to the asset, as with \code{\link[dataraft.core:dr_add_contract]{dataraft.core::dr_add_contract()}}.}

\item{asset}{Governed asset ID. Defaults to the selected node's name.}

\item{code_version}{Optional explicit code/dependency version. By default,
hashes normalized manifest node, source and macro definitions and dbt version,
excluding invocation IDs, timings and file locations. Include an explicit
version when additional dependencies are not represented in the manifest.}

\item{version}{Optional publication definition version. Omitted versions are
derived from the full publication definition, including its contract.}

\item{layer}{Configured destination layer. Defaults to \code{"marts"} if configured,
otherwise \code{"products"}. Supply a layer explicitly when neither exists.}

\item{business_date}{Business date, separate from build or publication time.}

\item{notify}{Optional existing notification callback.}

\item{stop_on_failure}{Signal failure after persisting quality evidence.}
}
\value{
A \code{dr_run_result} accepted by \code{\link[dataraft.core:dr_collect]{dataraft.core::dr_collect()}}. \verb{$outputs} identifies the
exact database, schema, immutable table, asset and release. Use \code{\link[dataraft.lake:dr_source_release]{dataraft.lake::dr_source_release()}}
to compose a consumer workflow; a dbt model relation remains mutable.
}
\description{
Requires a successful \verb{dbt build} result containing the selected materialized
node. Copies the relation currently visible to this connection into an
immutable candidate, validates its contract, then commits the release marker.
Omit the contract for structural checks only: column names and R types are
inferred, while empty data and missing values are allowed. Business keys,
quality rules, ownership and freshness are never guessed. Later dbt builds
cannot change this snapshot. The dbt invocation is
recorded as provenance, not as proof that the live relation has not changed
since that build. Coordinate writers between build and publication.
}
\details{
Each call validates a fresh snapshot; there is no cache based solely on a
dbt invocation ID. This prevents changed tables reusing stale quality
evidence.
This publishes one relation, not an atomic bundle of all dbt models.
}
\examples{
# After dr_dbt_build() and reopening the lake connection:
# release <- dr_dbt_publish(config, result, "customer_revenue")
# dataraft.core::dr_collect(release)
}
